import type React from "react"
import { MessageSquarePlus } from "lucide-react"

export const MessageIcon: React.FC = () => {
  return <MessageSquarePlus size={18} className="cursor-pointer text-gray-500 hover:text-gray-700" />
}
